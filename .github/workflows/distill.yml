# name: Distill Incidents

# on:
#   workflow_dispatch: {}
#   schedule:
#     - cron: "0 2 * * *"   # 02:00 UTC nightly

# jobs:
#   distill:
#     name: Distill incident proposals
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#       pull-requests: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Install uv
#         uses: astral-sh/setup-uv@v4
#         with:
#           version: "latest"
#           enable-cache: true

#       - name: Install dependencies
#         run: uv sync --frozen

#       - name: Run distiller (non-blocking)
#         # make distill || true — failures are logged but do not break CI.
#         # The distiller proposes new skills / verification rules from
#         # data/incidents/*.yaml and writes proposals to data/proposals/.
#         # Phase F will add a step that opens a PR from the proposals.
#         run: make distill || true
#         env:
#           AZURE_OPENAI_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
#           AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
#           AZURE_OPENAI_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_DEPLOYMENT }}
#           LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
#           LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
#           LANGFUSE_HOST: ${{ secrets.LANGFUSE_HOST }}

#       - name: Upload distill proposals as artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: distill-proposals-${{ github.run_id }}
#           path: data/proposals/
#           retention-days: 30
#           if-no-files-found: warn
